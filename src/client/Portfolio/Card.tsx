import * as React from 'react';
import RepoType from '../../types/Repository';

interface CardProps {
    repo: RepoType[0];
}
const Card: React.FC<CardProps> = props => {
    return null;
}

export default Card;